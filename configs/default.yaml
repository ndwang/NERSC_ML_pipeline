# Default configuration
# Compose from sub-configs by specifying paths

model: model/vae2d.yaml
training: training/default.yaml
data: data/v2_sectioned_1sec_10k.yaml

output_dir: ./runs
run_name: null  # Auto-generated if not specified
